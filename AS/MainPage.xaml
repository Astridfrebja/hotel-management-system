<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AS.MainPage">

    <ScrollView>
        <VerticalStackLayout
        Spacing="25"
        Padding="30"
        VerticalOptions="Center">

            <Label
            Text="Velg din rolle"
            FontSize="Title"
            HorizontalOptions="Center" />

            <Button
            Text="Renholder"
            Clicked="OnCleanerButtonClicked"
            HorizontalOptions="Fill" />

            <Button
            Text="Serviceperson"
            Clicked="OnServiceButtonClicked"
            HorizontalOptions="Fill" />

            <Button
            Text="Vedlikeholdsperson"
            Clicked="OnMaintainerButtonClicked"
            HorizontalOptions="Fill" />
        </VerticalStackLayout>
        
    </ScrollView>

</ContentPage>
